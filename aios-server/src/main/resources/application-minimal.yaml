server:
  port: 48080

spring:
  autoconfigure:
    # 排除所有可能导致问题的自动配置
    exclude:
      - cn.iocoder.aios.framework.datapermission.config.AiosDataPermissionAutoConfiguration
      - cn.iocoder.aios.framework.datapermission.config.AiosDeptDataPermissionAutoConfiguration
      - cn.iocoder.aios.framework.tenant.config.AiosTenantAutoConfiguration
      - cn.iocoder.aios.framework.security.config.AiosSecurityAutoConfiguration
      - com.alibaba.druid.spring.boot.autoconfigure.DruidDataSourceAutoConfigure
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration

# 芋道配置项
aios:
  security:
    mock-enable: true

# 数据库配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ry_vue?useUnicode=true&characterEncoding=utf8&zeroDateTimeBehavior=convertToNull&useSSL=false&allowPublicKeyRetrieption=true&serverTimezone=GMT%2B8
    username: wms
    password: Wms@2024
    driver-class-name: com.mysql.cj.jdbc.Driver

# 日志配置
logging:
  level:
    root: INFO